// A small mono eyebrow that sits above a serif heading. Oxblood, uppercase,
// wide tracking. One of the few sanctioned uses of the accent.
export function KickerLabel({ children }: { children: React.ReactNode }) {
  return <p className="kicker">{children}</p>;
}
